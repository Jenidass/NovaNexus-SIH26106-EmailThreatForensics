// Dashboard is fully self-contained (own full-bleed background, no nav
// expected from the host). Rendered as-is, unmodified.
import Dashboard from "../modules/dashboard/components/dashboard/Dashboard";

export default function DashboardRoute() {
  return <Dashboard />;
}
